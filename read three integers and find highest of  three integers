#include<stdio.h>
int main()
{
int a,b,c;
printf("enter the value of a=%d,b=%d,c=%d\n",a,b,c);
scanf("%d,%d,%d",&a,&b,&c);
if((a>=b) && (a>=c))
{
printf(" a is high\n");
}
else if((b>=c) && (b>=a))
{
printf(" b is high\n ");
}
else((c>=b) && (c>=a))
{
printf(" c is high\n ");
}
}
